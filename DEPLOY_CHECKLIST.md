# ✅ قائمة تحقق رفع الموقع للاستضافة

## 📦 قبل الرفع

### على الجهاز المحلي:
- [ ] تحديث ملف `.env` بإعدادات الإنتاج
- [ ] تشغيل `npm run build` لبناء assets
- [ ] اختبار الموقع محلياً بشكل كامل
- [ ] نسخ احتياطي من قاعدة البيانات (إن وجدت)

---

## 📤 أثناء الرفع

### رفع الملفات:
- [ ] رفع جميع ملفات `app/`
- [ ] رفع جميع ملفات `bootstrap/`
- [ ] رفع جميع ملفات `config/`
- [ ] رفع جميع ملفات `database/migrations/`
- [ ] رفع جميع ملفات `lang/`
- [ ] رفع جميع ملفات `public/` (إلى المجلد الرئيسي للاستضافة)
- [ ] رفع جميع ملفات `resources/`
- [ ] رفع جميع ملفات `routes/`
- [ ] رفع `artisan`
- [ ] رفع `composer.json` و `composer.lock`
- [ ] رفع `package.json` و `package-lock.json`
- [ ] رفع `vite.config.js`
- [ ] رفع `.htaccess` من `public/`

---

## 🔧 على السيرفر (بعد الرفع)

### الإعداد الأساسي:
- [ ] إنشاء ملف `.env` على السيرفر
- [ ] تحديث `APP_URL` في `.env`
- [ ] تحديث `APP_DEBUG=false` في `.env`
- [ ] تحديث إعدادات قاعدة البيانات في `.env`

### تثبيت Dependencies:
- [ ] تشغيل `composer install --optimize-autoloader --no-dev`
- [ ] تشغيل `npm install`
- [ ] تشغيل `npm run build`

### إعداد Laravel:
- [ ] تشغيل `php artisan key:generate` (إذا لزم الأمر)
- [ ] تشغيل `php artisan storage:link`
- [ ] تشغيل `php artisan migrate --force`

### Cache و Optimization:
- [ ] تشغيل `php artisan config:cache`
- [ ] تشغيل `php artisan route:cache`
- [ ] تشغيل `php artisan view:cache`
- [ ] تشغيل `php artisan optimize`

### الصلاحيات:
- [ ] تعيين صلاحيات `storage/` إلى 775
- [ ] تعيين صلاحيات `bootstrap/cache/` إلى 775

---

## 🗄️ قاعدة البيانات

- [ ] إنشاء قاعدة بيانات MySQL على السيرفر
- [ ] إنشاء مستخدم قاعدة بيانات
- [ ] تحديث بيانات الاتصال في `.env`
- [ ] تشغيل migrations بنجاح
- [ ] التحقق من إنشاء الجداول

---

## ✅ اختبارات بعد الرفع

### الصفحة الرئيسية:
- [ ] فتح الموقع في المتصفح
- [ ] الصفحة الرئيسية تظهر بشكل صحيح
- [ ] الصور تظهر
- [ ] CSS و JavaScript يعملان

### الوظائف:
- [ ] تغيير اللغة يعمل (عربي/إنجليزي)
- [ ] الروابط تعمل
- [ ] الصفحات تتحمّل بشكل صحيح

### لوحة التحكم:
- [ ] الوصول إلى `/admin/dashboard`
- [ ] عرض المشاريع
- [ ] عرض الخدمات
- [ ] إضافة مشروع جديد يعمل
- [ ] تعديل مشروع يعمل
- [ ] حذف مشروع يعمل
- [ ] إضافة خدمة جديدة يعمل
- [ ] تعديل خدمة يعمل
- [ ] حذف خدمة يعمل

### رفع الصور:
- [ ] رفع صورة مشروع يعمل
- [ ] عرض صورة المشروع يعمل
- [ ] الصور المحفوظة تظهر بشكل صحيح

---

## 🔐 الأمان

- [ ] `APP_DEBUG=false` في `.env`
- [ ] `APP_ENV=production` في `.env`
- [ ] ملف `.env` غير قابل للوصول من المتصفح
- [ ] صلاحيات الملفات صحيحة
- [ ] SSL Certificate مثبت (HTTPS)

---

## 📝 ملاحظات إضافية

- [ ] التحقق من Laravel logs عند حدوث أخطاء
- [ ] التحقق من server logs
- [ ] نسخ احتياطي دوري من قاعدة البيانات
- [ ] مراقبة استخدام الموارد

---

**تاريخ الرفع:** _______________

**اسم الدومين:** _______________

**ملاحظات:** 
_______________________________________________
_______________________________________________

